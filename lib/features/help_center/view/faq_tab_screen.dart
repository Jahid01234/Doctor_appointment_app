import 'package:flutter/material.dart';

class FaqTabScreen extends StatelessWidget {
  const FaqTabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Faq Section.........."),),
    );
  }
}
